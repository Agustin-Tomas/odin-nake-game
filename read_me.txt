src
    main -> main loop

    entities -> state structs
    inputs -> input events
    renderer
        draw ->
          shaders -> after effects


Comando para buildear:
odin build . -out:"../bin/Snake Milk.exe" -file -subsystem:windows